import { RefreshTokenStrategy } from './refreshToken.strategy';
import { AccessTokenStrategy } from './accessToken.strategy';

// TODO: Тут, вроде, можно как-то иначе сделать. Посмотри библиотеку компонентов
export { RefreshTokenStrategy, AccessTokenStrategy };
